# 情报分析智能体 — 开发文档

> Intelligence Analysis Agent — Development Documentation
> 版本：v1.0.0 | 最后更新：2026-07-31

---

## 目录

- [一、项目概述](#一项目概述)
- [二、系统架构](#二系统架构)
- [三、目录结构](#三目录结构)
- [四、核心模块详解](#四核心模块详解)
  - [4.1 知识图谱 (KnowledgeGraph)](#41-知识图谱-knowledgegraph)
  - [4.2 向量数据库 (VectorDatabase)](#42-向量数据库-vectordatabase)
  - [4.3 LLM 客户端 (LLMClient)](#43-llm-客户端-llmclient)
  - [4.4 时序记忆库 (TimeSeriesMemory)](#44-时序记忆库-timeseriesmemory)
- [五、核心算法详解](#五核心算法详解)
  - [5.1 NATO 6×6 情报评估](#51-nato-66-情报评估)
  - [5.2 GraphRAG 交叉验证](#52-graphrag-交叉验证)
  - [5.3 DOTMLPF-P 薄弱点推演](#53-dotmlpf-p-薄弱点推演)
  - [5.4 异常检测](#54-异常检测)
  - [5.5 ACH 竞争性假说分析](#55-ach-竞争性假说分析)
  - [5.6 TRL 轨迹预测](#56-trl-轨迹预测)
- [六、数据采集与知识图谱构建管道](#六数据采集与知识图谱构建管道)
  - [6.1 管道总体流程](#61-管道总体流程)
  - [6.2 数据连接器](#62-数据连接器)
  - [6.3 LLM 实体/关系抽取](#63-llm-实体关系抽取)
  - [6.4 验证与入库](#64-验证与入库)
  - [6.5 向量数据库集成](#65-向量数据库集成)
- [七、多智能体红队对抗](#七多智能体红队对抗)
- [八、数据模型参考](#八数据模型参考)
- [九、CLI 使用指南](#九cli-使用指南)
- [十、配置与部署](#十配置与部署)
- [十一、测试](#十一测试)
- [十二、已知限制与后续规划](#十二已知限制与后续规划)

---

## 一、项目概述

**情报分析智能体**是一个战略级军事情报分析系统，从海量公开网络信息（OSINT）中提取真实情报，推演敌方"薄弱环节"与"未来走向"。

### 设计原则

| 原则 | 说明 |
|------|------|
| **证据链优先** | 任何结论必须可溯源到至少两个独立信息源 |
| **多视角对抗** | 内置"魔鬼代言人"机制，防止证实偏差 |
| **去伪存真** | NATO 6×6 评级 + GraphRAG 交叉验证 + 战略欺骗检测 |
| **结构化推理** | DOTMLPF-P 框架 + ACH 竞争性假说分析 |

### 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 感知层 | requests / BeautifulSoup / PyMuPDF / feedparser | 网页/PDF/RSS 数据采集 |
| 存储层 | Neo4j（图）/ Qdrant（向量）/ 内存模式 | 知识图谱 + 向量检索 |
| 认知层 | Qwen-2-72B / Llama-3-70B（本地部署，OpenAI 兼容 API） | LLM 推理与抽取 |
| 逻辑层 | 自研多智能体框架 | 红蓝对抗编排 |
| 评估层 | NATO 6×6 / GraphRAG / DOTMLPF-P / ACH / TRL | 情报评估与推演 |

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    CLI 入口 (main.py)                     │
│         demo / analyze / ingest / test 四种模式           │
└────────┬──────────┬──────────────┬───────────────────────┘
         │          │              │
    ┌────▼────┐ ┌───▼────┐  ┌─────▼──────────────────┐
    │ 分析模式 │ │ 演示模式│  │ 数据采集模式 (ingest)  │
    │ analyze │ │  demo  │  │                        │
    └────┬────┘ └───┬────┘  │ WebScraperConnector    │
         │          │       │ PDFParserConnector     │
         │          │       │ FeedParserConnector    │
         │          │       └─────┬──────────────────┘
         │          │             │
    ┌────▼──────────▼──────┐  ┌──▼──────────────────┐
    │  多智能体红队对抗     │  │ EntityExtractor     │
    │  (agents/)           │  │ (LLM 实体/关系抽取)  │
    │                      │  └──┬──────────────────┘
    │ BlueAgent            │     │
    │ RedAgent             │  ┌──▼──────────────────┐
    │ JudgeAgent           │  │ NATO 6×6 评估       │
    │ RedTeamOrchestrator  │  │ GraphRAG 交叉验证   │
    └────┬─────────────────┘  │ 冲突/欺骗检测       │
         │                    └──┬──────────────────┘
    ┌────▼───────────────────────▼──────────────────┐
    │              核心基础设施 (core/)               │
    │                                               │
    │  KnowledgeGraph    VectorDatabase             │
    │  TimeSeriesMemory  LLMClient                  │
    └───────────────────────────────────────────────┘
```

### 数据流向

```
原始数据（网页/PDF/RSS）
    │
    ▼
连接器采集 → Document（原始文档）
    │
    ▼
文本分块 → DocumentChunk[]
    │
    ▼
LLM 抽取 → ExtractedEntity[] + ExtractedRelation[]
    │
    ▼
NATO 6×6 评估 → AssessmentResult（来源可靠度 + 信息可信度）
    │
    ▼
GraphRAG 交叉验证 → 冲突检测 / 战略欺骗标记
    │
    ├─→ KnowledgeGraph（节点 + 边入库）
    ├─→ TimeSeriesMemory（时序记录）
    └─→ VectorDatabase（向量嵌入）
    │
    ▼
分析智能体群（基于图谱推理）
    │
    ▼
红蓝对抗仲裁 → 带置信度的情报结论
```

---

## 三、目录结构

```
Agent_VibeCoding_ver/
├── main.py                          # CLI 主入口（4 种运行模式）
├── __init__.py                      # 包定义与版本信息
├── requirements.txt                 # 依赖清单
├── 开发文档1.md                      # 架构设计哲学文档
├── 开发文档2.md                      # 本文档（详细开发文档）
│
├── core/                            # 核心模块
│   ├── __init__.py                  # 导出所有核心 API
│   ├── infrastructure.py            # 基础设施：KnowledgeGraph, VectorDatabase,
│   │                                #   LLMClient, TimeSeriesMemory
│   └── algorithms.py                # 核心算法：NATO 6×6, GraphRAG, DOTMLPF-P,
│                                    #   异常检测, ACH, TRL
│
├── agents/                          # 多智能体模块
│   ├── __init__.py
│   └── red_teaming.py               # BlueAgent, RedAgent, JudgeAgent,
│                                    #   RedTeamOrchestrator
│
├── ingestion/                       # 数据采集与知识图谱构建管道
│   ├── __init__.py
│   ├── models.py                    # 数据模型：Document, ExtractedEntity,
│   │                                #   IngestionResult 等
│   ├── pipeline.py                  # IngestionPipeline 主编排管道
│   ├── connectors/                  # 数据源连接器
│   │   ├── __init__.py
│   │   ├── base.py                  # BaseConnector 抽象基类
│   │   ├── web_scraper.py           # 网页采集（requests + BeautifulSoup）
│   │   ├── pdf_parser.py            # PDF 解析（PyMuPDF/fitz）
│   │   └── feed_parser.py           # RSS/Atom 订阅（feedparser）
│   └── extraction/                  # LLM 实体/关系抽取
│       ├── __init__.py
│       ├── entity_extractor.py      # EntityExtractor 抽取器
│       └── prompts.py               # LLM 提示词模板
│
└── tests/                           # 单元测试
    ├── __init__.py
    ├── test_algorithms.py           # 核心算法测试（36 个用例）
    └── test_ingestion.py            # 采集管道测试（58 个用例）
```

---

## 四、核心模块详解

### 4.1 知识图谱 (KnowledgeGraph)

**文件**：`core/infrastructure.py`

知识图谱是系统的核心存储与推理基础设施，支持内存模式和可选的 Neo4j 后端。

#### 数据结构

```python
@dataclass
class GraphNode:
    id: str                          # 节点唯一标识
    type: str                        # 类型：Weapon / Organization / Person /
                                     #   Event / Location / Technology / Budget
    name: str                        # 实体名称
    attributes: dict[str, Any]       # 属性键值对（射程、速度、数量等）
    confidence: float                # 置信度 [0, 1]
    sources: list[str]               # 信息来源列表
    created_at: str                  # 创建时间（ISO 8601）

@dataclass
class GraphEdge:
    source_id: str                   # 源节点 ID
    target_id: str                   # 目标节点 ID
    relation: str                    # 关系类型（developed_by / deployed_at 等）
    confidence: float                # 置信度 [0, 1]
    evidence: list[str]              # 证据列表
```

#### API 参考

| 方法 | 说明 |
|------|------|
| `add_node(node)` | 添加节点，已存在则合并属性（取最高置信度，来源去重） |
| `add_edge(edge)` | 添加边，自动去重（同源同目标同关系只保留一条） |
| `query(entity, attribute, entity_type, time_window)` | 多条件查询节点 |
| `get_related_nodes(node_id, max_depth)` | BFS 遍历获取关联节点 |
| `get_entity_facts(entity_name, attribute)` | 获取实体事实列表 |
| `find_node_by_name(name)` | **O(1) 名称索引查找**（优先名称 → 回退 ID） |
| `get_edges_for_node(node_id)` | 获取节点关联的所有边 |
| `get_statistics()` | 图谱统计（节点/边数、类型分布、平均置信度） |
| `export_snapshot()` | 导出 JSON 可序列化的图谱快照 |
| `import_snapshot(snapshot)` | 从快照导入图谱数据 |
| `load_from_neo4j()` | 从 Neo4j 加载数据到内存 |

#### 使用示例

```python
from core import KnowledgeGraph, GraphNode, GraphEdge

kg = KnowledgeGraph()  # 内存模式
# kg = KnowledgeGraph(neo4j_uri="bolt://localhost:7687",
#                     neo4j_user="neo4j", neo4j_password="password")

# 添加实体
kg.add_node(GraphNode(
    id="weapon_df21d", type="Weapon", name="DF-21D 反舰弹道导弹",
    attributes={"range_km": 1500, "speed_mach": 3.0},
    confidence=0.9, sources=["简氏防务周刊"]
))

# 添加关系
kg.add_edge(GraphEdge(
    source_id="weapon_df21d", target_id="org_casic",
    relation="developed_by", confidence=0.85
))

# 查询
facts = kg.get_entity_facts("DF-21D 反舰弹道导弹", "range_km")
# → [{'entity': 'DF-21D 反舰弹道导弹', 'attribute': 'range_km',
#     'value': 1500, 'confidence': 0.9, ...}]

# 导出快照（用于持久化/传输）
snapshot = kg.export_snapshot()
```

### 4.2 向量数据库 (VectorDatabase)

**文件**：`core/infrastructure.py`

支持 Qdrant 后端和内存模式（余弦相似度搜索）。

```python
from core import VectorDatabase

vdb = VectorDatabase()  # 内存模式
vdb.store("entity_001", [0.1, 0.2, ...], {"name": "DF-21D", "type": "Weapon"})
results = vdb.search([0.1, 0.2, ...], top_k=5, score_threshold=0.7)
# → [("entity_001", 0.95, {"name": "DF-21D", ...})]
```

### 4.3 LLM 客户端 (LLMClient)

**文件**：`core/infrastructure.py`

支持本地部署的 Qwen-2-72B 或 Llama-3-70B（通过 OpenAI 兼容 API）。

```python
from core import LLMClient, LLMConfig

llm = LLMClient(LLMConfig(
    model_name="qwen2-72b",
    api_base="http://localhost:8000/v1",
    api_key="local",
    temperature=0.3,
    max_tokens=4096,
))

# 异步调用
response = await llm.generate("分析这段文本", system_prompt="你是情报分析专家")

# 同步调用
response = llm.generate_sync("分析这段文本")
```

当 `openai` 包未安装或 API 不可用时，自动回退到 `_mock_generate()` 模拟模式。

### 4.4 时序记忆库 (TimeSeriesMemory)

**文件**：`core/infrastructure.py`

用于存储情报分析中的时序信息，支持弱信号检测和趋势分析。

```python
from core import TimeSeriesMemory
from datetime import datetime

memory = TimeSeriesMemory(max_entries=10000)
memory.add(
    content="DF-21D 批量采购",
    metadata={"key": "budget", "value": "DF-21D", "amount": 5000000},
    timestamp=datetime(2024, 3, 15)
)

# 查询
entries = memory.query(key="budget", value="DF-21D")

# 获取时间线
timeline = memory.get_timeline(key="budget", value="DF-21D", interval="monthly")
```

---

## 五、核心算法详解

### 5.1 NATO 6×6 情报评估

**文件**：`core/algorithms.py` · 函数 `assess_evidence()`

采用北约标准的双重评估体系：

| 维度 | 等级 | 说明 |
|------|------|------|
| **来源可靠度 (SR)** | A-F | A=极其可靠, B=相当可靠, C=一般可靠, D=可疑, E=不可靠, F=无法评定 |
| **信息可信度 (IC)** | 1-6 | 6=极其可信, 5=相当可信, 4=一般可信, 3=不可信, 2=极不可信, 1=无法确认 |

**计算公式**：

```
SR_score = 0.4 × history_accuracy + 0.3 × expertise_level
           + 0.2 × independence + 0.1 × traceability

IC_score = 0.3 × normalized_cross_ref_count
           + 0.4 × avg_cross_ref_reliability
           + 0.3 × consistency

combined_confidence = 0.4 × SR_NORM[sr] + 0.6 × IC_NORM[ic]
```

**Maskirovka（战略欺骗）检测**：当新事实与历史数据存在 >30% 的数值冲突，且来源可靠度为 D/E/F 时，自动标记。

### 5.2 GraphRAG 交叉验证

**文件**：`core/algorithms.py` · 函数 `cross_validate_fact()`

在知识图谱中查找与新事实相关的历史事实，计算冲突分数：

| 场景 | 冲突分数 | 处理方式 |
|------|----------|----------|
| 数值完全一致 | 0.0 | 事实确认 |
| 数值偏差 < 10% | 0.0-0.1 | 事实确认 |
| 数值偏差 10%-30% | 0.1-0.3 | 低冲突，记录但不告警 |
| 数值偏差 > 30% | 0.3-1.0 | 高冲突，生成 EntityConflict |
| 最大冲突 > 50% 且平均 > 30% | — | 标记为疑似战略欺骗 |

### 5.3 DOTMLPF-P 薄弱点推演

**文件**：`core/algorithms.py` · 函数 `deduce_vulnerabilities()`

基于 8 维度军事评估框架：

| 维度 | 缩写 | 分析方向 |
|------|------|----------|
| 条令 | Doctrine | 作战条令、战术规范 |
| 组织 | Organization | 编制结构、指挥链 |
| 训练 | Training | 培训体系、演练频率 |
| 物资 | Materiel | 装备数量、技术参数 |
| 领导力 | Leadership | 指挥官素质、决策能力 |
| 人员 | Personnel | 兵员数量、专业素质 |
| 设施 | Facilities | 基地、试验场、维护设施 |
| 政策 | Policy | 国防政策、预算分配 |

每个维度的薄弱度评分通过 sigmoid 归一化：

```
weakness_score = 1 / (1 + exp(-(weakness_count - strength_count) / scale))
```

### 5.4 异常检测

三种异常检测算法：

| 算法 | 函数 | 检测目标 |
|------|------|----------|
| 采购异常 | `detect_procurement_anomaly()` | 采购金额/频率的统计异常 + 新品类语义异常 |
| 学术引用异常 | `detect_academic_citation_anomaly()` | 论文引用频率突增 |
| 社交媒体异常 | `detect_social_anomaly()` | 负面情感 → 社会不安定信号 |

### 5.5 ACH 竞争性假说分析

**文件**：`core/algorithms.py` · 函数 `ach_analysis()`

完整的 ACH 分析流程：

```
1. generate_hypotheses()    → 生成 3-4 个互斥假说
2. build_evidence_matrix()  → 证据-假说矩阵（支持/反对/中性/单源）
3. calculate_probability_distribution() → 贝叶斯概率分布
```

输出示例：
```json
{
  "hypotheses": [
    {"description": "加速发展", "probability": 0.65, "evidence_count": 8},
    {"description": "维持现状", "probability": 0.20, "evidence_count": 3},
    {"description": "转向替代方案", "probability": 0.15, "evidence_count": 2}
  ]
}
```

### 5.6 TRL 轨迹预测

**文件**：`core/algorithms.py` · 函数 `forecast_trajectory()`

整合四种分析技术：

| 技术 | 说明 |
|------|------|
| TRL 评估 | 技术成熟度等级（1-9 级） |
| 预算趋势 | 预算增减方向与速率 |
| 弱信号检测 | 频率异常 + 时间聚类 |
| ACH 分析 | 概率化未来走向 |

---

## 六、数据采集与知识图谱构建管道

### 6.1 管道总体流程

**文件**：`ingestion/pipeline.py` · 类 `IngestionPipeline`

```
Document（原始文档）
    │
    ├─ 步骤 1: _step_extract()        → LLM 抽取实体和关系
    │
    ├─ 步骤 2: _step_validate()        → NATO 6×6 评估
    │
    ├─ 步骤 3: _step_cross_validate()  → GraphRAG 交叉验证
    │
    ├─ 步骤 4: _step_ingest_entity()   → 知识图谱节点入库
    │
    ├─ 步骤 5: _step_ingest_relation() → 知识图谱边入库
    │
    ├─ 步骤 6: _store_to_memory()      → 时序记忆库存储
    │
    └─ 步骤 7: _store_to_vector_db()   → 向量数据库存储
```

#### 使用示例

```python
from core import KnowledgeGraph, LLMClient, LLMConfig, TimeSeriesMemory, VectorDatabase
from ingestion import IngestionPipeline
from core import SourceInfo, SourceReliability

# 初始化
kg = KnowledgeGraph()
llm = LLMClient(LLMConfig(model_name="qwen2-72b", api_base="http://localhost:8000/v1"))
memory = TimeSeriesMemory()
vdb = VectorDatabase()

pipeline = IngestionPipeline(kg, llm, memory=memory, vector_db=vdb)

# 定义来源信息
source_info = SourceInfo(
    name="简氏防务周刊",
    reliability=SourceReliability.A,
    history_accuracy=0.95,
    expertise_level=0.9,
    independence=0.8,
    traceability=True,
)

# 批量入库
result = await pipeline.ingest_from_urls(
    urls=["https://www.janes.com/defence-news/article-123"],
    source_info=source_info,
)

print(f"入库实体: {result.entity_count}, 关系: {result.relation_count}")
print(f"冲突: {result.conflict_count}, 战略欺骗嫌疑: {result.has_maskirovka}")
```

### 6.2 数据连接器

| 连接器 | 文件 | 数据源 | 关键特性 |
|--------|------|--------|----------|
| `WebScraperConnector` | `web_scraper.py` | 网页 | User-Agent 轮换、SSRF 全范围防护、非阻塞 I/O |
| `PDFParserConnector` | `pdf_parser.py` | PDF 文件 | 按页提取、元数据解析、try/finally 资源管理 |
| `FeedParserConnector` | `feed_parser.py` | RSS/Atom | 逐条采集(`fetch_entries`)、日期解析 |

#### SSRF 防护

`WebScraperConnector` 阻止以下地址范围：

- `localhost`, `127.0.0.1`, `::1`（环回）
- `10.0.0.0/8`（A 类私有）
- `172.16.0.0/12`（B 类私有）
- `192.168.0.0/16`（C 类私有）
- `169.254.0.0/16`（链路本地，含 AWS 元数据 `169.254.169.254`）
- `0.0.0.0`（未指定地址）
- 所有 IPv6 私有/环回/链路本地范围

使用 `ipaddress` 标准库进行精确判断，支持 IPv4 和 IPv6。

#### 异步非阻塞 I/O

所有连接器的 `fetch()` 方法使用 `asyncio.to_thread()` 包装阻塞调用，避免阻塞事件循环。

### 6.3 LLM 实体/关系抽取

**文件**：`ingestion/extraction/entity_extractor.py` · 类 `EntityExtractor`

#### 抽取流程

```
原始文本
    │
    ▼
chunk_text() → DocumentChunk[]（1000字符/块，200字符重叠）
    │
    ▼（asyncio.gather 并行处理所有分块）
    │
    ├─ _extract_entities_from_chunk()  → 实体 JSON → ExtractedEntity[]
    │     │
    │     └─ JSON 解析失败时：markdown 提取 → {...} 提取 → 正则回退
    │
    └─ _extract_relations_from_chunk() → 关系 JSON → ExtractedRelation[]
          │
          └─ JSON 解析失败时：markdown 提取 → {...} 提取 → 正则回退
    │
    ▼
_deduplicate_entities()   → 按 entity_id 去重，保留最高置信度
_deduplicate_relations()  → 按 (src, tgt, type) 去重，保留最高置信度
```

#### JSON 解析三级回退

1. **直接 JSON 解析**：`json.loads(response)`
2. **Markdown 代码块提取**：正则匹配 `` ```json {...} ``` ``
3. **花括号块提取**：正则匹配 `{...}`
4. **正则回退**（实体和关系均支持）：匹配 `"name": "..."` / `"source_entity": "..."` 等模式，置信度 0.3

#### 实体类型

| 类型 | 说明 | 典型属性 |
|------|------|----------|
| Weapon | 武器系统 | range_km, speed_mach, deployed_units |
| Organization | 军事/政府/科研机构 | role, established, employees |
| Person | 关键人物 | title, specialty |
| Event | 军事/政治事件 | date, location, scale |
| Location | 地理位置 | coordinates, type, purpose |
| Technology | 技术/子系统 | maturity, performance |
| Budget | 预算/财务记录 | amount, year, category |

#### 关系类型

| 关系 | 说明 |
|------|------|
| developed_by | 武器/技术由某机构研发 |
| deployed_at | 武器/部队部署在某地点 |
| depends_on | 系统依赖某技术/资源 |
| commanded_by | 部队由某人指挥 |
| funded_by | 项目由某预算/机构资助 |
| tested_at | 武器在试验场测试 |
| has_issue | 系统存在已知问题 |
| requires | 系统需要某训练/资源 |
| located_in | 地点位于某区域 |
| competes_with | 项目与替代方案竞争 |
| participates_in | 人员/单位参与事件 |
| succeeded_by | 系统被后续版本替代 |

### 6.4 验证与入库

每个抽取的实体经过以下验证后才入库：

```
ExtractedEntity
    │
    ├─ NATO 6×6 评估
    │   └─ assess_evidence() → 来源可靠度 + 信息可信度 + 综合置信度
    │
    ├─ GraphRAG 交叉验证（逐属性）
    │   └─ cross_validate_fact() → 冲突分数 + 战略欺骗标记
    │       ├─ 无冲突 → 正常入库
    │       ├─ 低冲突 (0.1-0.3) → 入库 + 记录冲突
    │       └─ 高冲突 (>0.3) → 入库 + 标记 EntityConflict
    │           └─ maskirovka_flag=True → 疑似战略欺骗
    │
    └─ 置信度调整
        adjusted_confidence = entity.confidence × SR_NORM[source_reliability]
```

**实体入库到知识图谱时**：
- 新实体 → 创建 GraphNode
- 已存在实体 → 合并属性、取最高置信度、来源去重

**关系入库**：
- 通过 ID 或名称解析两端实体
- 仅两端都存在时入库
- 自动去重（同源同目标同关系只保留一条）

### 6.5 向量数据库集成

实体入库时同步存储到向量数据库：

- **实体向量**：基于实体名称、类型、属性生成 128 维特征向量
- **文档分块向量**：基于文本 n-gram 哈希生成稀疏向量

> 当前使用文本哈希伪向量（适用于内存模式）。生产环境建议集成 `sentence-transformers` 生成真实语义嵌入。

---

## 七、多智能体红队对抗

**文件**：`agents/red_teaming.py`

```
                    ┌──────────────┐
                    │ 分析主题输入  │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
              ┌────▶│  Blue Agent  │◀─────┐
              │     │ (情报综合)    │      │
              │     └──────┬───────┘      │
              │            │ 初步结论      │ 补充证据
              │     ┌──────▼───────┐      │
              │     │  Red Agent   │      │
              │     │ (魔鬼代言人)  │      │
              │     └──────┬───────┘      │
              │            │ 质疑挑战      │
              │     ┌──────▼───────┐      │
              │     │ Judge Agent  │──────┘
              │     │  (仲裁者)    │ 要求补充
              │     └──────┬───────┘
              │            │
              │     ≤ 3 轮博弈
              │            │
              └────────────┘
                           │
                    ┌──────▼───────┐
                    │  最终结论     │
                    │  + 薄弱点     │
                    │  + 置信度     │
                    └──────────────┘
```

| 智能体 | 角色 | 职责 |
|--------|------|------|
| **BlueAgent** | 情报综合 | 汇总知识图谱事实，使用 ACH 法输出初步推断 |
| **RedAgent** | 魔鬼代言人 | 质疑 Blue 的结论，寻找逻辑漏洞和孤证 |
| **JudgeAgent** | 仲裁者 | 权衡双方论点，剔除证据不足的推断 |
| **RedTeamOrchestrator** | 编排器 | 管理最多 3 轮对抗博弈 |

```python
from agents import BlueAgent, RedAgent, JudgeAgent, RedTeamOrchestrator

blue = BlueAgent(llm, kg)
red = RedAgent(llm, kg)
judge = JudgeAgent(llm)
orchestrator = RedTeamOrchestrator(blue, red, judge, max_rounds=3)

result = await orchestrator.debate("DF-21D 反舰弹道导弹发展")
# result 包含:
#   final_conclusion: {confidence, main_points}
#   vulnerabilities: [{dimension, weakness_score, description}]
#   resolved_challenges: [{type, description}]
#   debate_rounds: int
```

---

## 八、数据模型参考

### 管道数据模型 (`ingestion/models.py`)

| 模型 | 说明 |
|------|------|
| `Document` | 原始文档（url, title, content, doc_type, source_name, timestamp） |
| `DocumentChunk` | 文档分块（document_url, content, chunk_index, start_pos, end_pos） |
| `ExtractedEntity` | 抽取的实体（entity_id, entity_type, name, attributes, confidence） |
| `ExtractedRelation` | 抽取的关系（source_entity, target_entity, relation_type, confidence） |
| `ExtractionResult` | 单次抽取结果（entities, relations, raw_response, parse_success） |
| `EntityConflict` | 冲突记录（entity_name, attribute, new_value, old_value, conflict_score, maskirovka_flag） |
| `IngestionResult` | 入库结果汇总（document_count, entity_count, relation_count, conflicts, warnings, status） |

### 枚举类型

| 枚举 | 值 |
|------|-----|
| `DocumentType` | WEB_PAGE, PDF, RSS_FEED, PLAIN_TEXT |
| `IngestionStatus` | PENDING, EXTRACTING, VALIDATING, INGESTED, FAILED |

### 算法数据模型 (`core/algorithms.py`)

| 模型 | 说明 |
|------|------|
| `SourceInfo` | 信息源（name, reliability, history_accuracy, expertise_level, independence） |
| `Evidence` | 证据（content, source, timestamp, entity, attribute, value, cross_references） |
| `AssessmentResult` | NATO 评估结果（source_reliability, info_credibility, combined_confidence, maskirovka_flag） |
| `Hypothesis` | ACH 假说（description, evidence_evaluations） |
| `Vulnerability` | 薄弱点（dimension, weakness_score, evidence_count, description） |
| `TRLLevel` | 技术成熟度等级（1-9 级） |

---

## 九、CLI 使用指南

### 安装依赖

```bash
pip install -r requirements.txt
```

### 运行模式

#### 演示模式（无需本地 LLM）

```bash
python main.py --mode demo
```

使用模拟数据运行完整分析流程，输出保存到 `analysis_result.json`。

#### 数据采集与知识图谱构建

```bash
# 使用真实 LLM（需要本地 Qwen-2-72B 服务）
python main.py --mode ingest \
    --sources "web:https://defense-news.com/article" \
              "feed:https://military-review.com/rss" \
              "pdf:./reports/defense-report-2024.pdf" \
    --source-name "简氏防务周刊" \
    --source-reliability A \
    --output ingest_result.json

# 使用 Mock LLM 测试（不需要本地 LLM 服务）
python main.py --mode ingest \
    --sources "web:https://example.com/article" \
    --demo \
    --output ingest_result.json
```

**参数说明**：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--sources` / `-s` | 数据源列表，格式 `type:path`（type: web/feed/pdf） | 必填 |
| `--source-name` | 来源名称（用于 NATO 评级） | "OSINT 公开来源" |
| `--source-reliability` | 来源可靠度等级 A-F | C |
| `--demo` | 使用 Mock LLM（测试用） | False |
| `--output` / `-o` | 输出文件路径 | analysis_result.json |

#### 情报分析模式

```bash
python main.py --mode analyze \
    --topic "DF-21D 反舰弹道导弹发展" \
    --output analysis_result.json
```

> 需要本地 LLM 服务运行在 `http://localhost:8000/v1`。

#### 运行测试

```bash
python main.py --mode test
```

---

## 十、配置与部署

### LLM 配置

系统默认连接本地部署的 Qwen-2-72B，通过 OpenAI 兼容 API 调用：

```python
LLMConfig(
    model_name="qwen2-72b",        # 模型名称
    api_base="http://localhost:8000/v1",  # API 地址
    api_key="local",                # API 密钥（本地部署可任意）
    temperature=0.3,                # 温度（情报分析偏低，减少幻觉）
    max_tokens=4096,                # 最大输出 token 数
    top_p=0.9,                      # 核采样
)
```

支持的 LLM 后端：
- **vLLM** + Qwen-2-72B（推荐）
- **Ollama** + Llama-3-70B
- 任何兼容 OpenAI API 的推理框架

### Neo4j 配置（可选）

```python
kg = KnowledgeGraph(
    neo4j_uri="bolt://localhost:7687",
    neo4j_user="neo4j",
    neo4j_password="your_password"
)
```

不配置时自动使用内存模式。

### Qdrant 配置（可选）

```python
vdb = VectorDatabase(host="localhost", port=6333)
```

不配置时自动使用内存模式。

---

## 十一、测试

### 运行全部测试

```bash
python -m pytest tests/ -v
```

### 测试覆盖

| 测试文件 | 测试类 | 用例数 | 覆盖内容 |
|----------|--------|--------|----------|
| `test_algorithms.py` | TestNATOAssessment | 6 | NATO 6×6 可靠度/可信度/评估 |
| | TestGraphRAG | 4 | 交叉验证（确认/冲突/无历史/更新） |
| | TestDOTMLPFP | 5 | 维度评分/薄弱点推演/综合 |
| | TestAnomalyDetection | 4 | 采购/学术/社交异常检测 |
| | TestACHAnalysis | 4 | 假说生成/证据矩阵/概率/完整分析 |
| | TestTLRPrediction | 5 | TRL/预算趋势/弱信号 |
| | TestRedTeaming | 4 | Blue/Red/Judge/编排器 |
| | TestInfrastructure | 3 | 知识图谱CRUD/向量库/时序记忆 |
| | TestIntegration | 1 | 完整分析管道 |
| `test_ingestion.py` | TestModels | 9 | 数据模型创建/合并/属性 |
| | TestConnectors | 6 | URL/路径验证 |
| | TestEntityExtractor | 12 | 分块/JSON解析/正则回退/去重 |
| | TestIngestionPipeline | 11 | 管道创建/连接器检测/入库/记忆 |
| | TestIngestionIntegration | 4 | 预填充图谱/手动入库/关系入库 |
| | TestKnowledgeGraphEnhanced | 6 | 边去重/名称索引/统计/快照 |
| | TestExtractorEnhanced | 4 | document_url/关系正则回退 |
| | TestConnectorsEnhanced | 2 | SSRF 完整防护 |
| | TestPipelineEnhanced | 4 | 旧值提取/向量存储/文本向量 |
| **合计** | | **94** | |

### 编写新测试

```python
import pytest
import asyncio
from core.infrastructure import KnowledgeGraph, GraphNode
from ingestion.pipeline import IngestionPipeline

class TestMyFeature:
    def test_something(self):
        kg = KnowledgeGraph()
        kg.add_node(GraphNode(id="test", type="Test", name="测试"))
        assert kg.find_node_by_name("测试") is not None

    def test_async_feature(self):
        result = asyncio.run(some_async_function())
        assert result is not None
```

---

## 十二、已知限制与后续规划

### 当前限制

| 限制 | 说明 | 影响 |
|------|------|------|
| 向量伪嵌入 | 使用文本哈希而非语义嵌入 | 语义搜索精度有限 |
| 文件类型 | 仅支持 PDF，不支持 .txt/.docx | 数据源覆盖不全 |
| 实体消歧 | 同实体不同名称（如 "DF-21D" / "东风-21D"）不会合并 | 图谱碎片化 |
| OCR | PDF 仅文本提取，无 OCR | 扫描件无法处理 |
| RSS 检测 | 仅基于 URL 关键词判断 feed | 可能误判 |

### 后续规划

| 优先级 | 规划 | 说明 |
|--------|------|------|
| P0 | 集成 sentence-transformers | 替换伪向量为真实语义嵌入 |
| P0 | 实体消歧/共指解析 | LLM 辅助合并同义实体 |
| P1 | 支持 .txt / .docx / .html | 扩展文件连接器 |
| P1 | 添加 OCR 支持 | 集成 Tesseract/EasyOCR |
| P2 | 知识图谱可视化 | 前端图谱展示界面 |
| P2 | 增量采集 | 避免重复采集已处理的文档 |
| P3 | 分布式部署 | 多节点采集 + 中心图谱合并 |
